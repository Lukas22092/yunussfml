{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug template",
            "type": "lldb",
            "request": "launch",
            "program": "${workspaceFolder}/build/template",
            "args": [],
            "cwd": "${workspaceFolder}",
            "preLaunchTask": "Build"
        }
    ]
}
